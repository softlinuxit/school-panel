{
  "name": "app",
  "version": "0.0.0",
  "scripts": {
    "build:production": "cd ./frontend && npm install && npm run build:production && cd ../backend && npm install",
    "start:production": "cd ./backend && npm run start:production",
    "setup:production": "cd ./backend && npm run setup:production"
  }
}